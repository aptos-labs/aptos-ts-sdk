{
  "name": "web-sdk-test",
  "version": "1.0.0",
  "description": "Test Aptos TypeScript SDK in web/browser environment using Vitest + happy-dom",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "link:../.."
  },
  "devDependencies": {
    "happy-dom": "^18.0.1",
    "vitest": "^3.2.4"
  }
}
